"""Lower-layer economic fixtures, never public-tape eligibility evidence.

The public Paper entry rejects unproven groups. These helpers exercise the
existing queue/account durable kernel with explicitly ordered synthetic input;
they do not patch a validator or prove that any real archive can fill an order.
"""


def model_trade(processor, trade):
    return processor._process_ordered_model_trades((trade,))
